#include<stdio.h>
#include<string.h>
int main()
{
    int length;
    char str[100];
    printf("enter the string and is length  :");
    gets(str);
    length=strlen(str);
    printf("the length of the string :%d\n",length);
    return 0;
}
